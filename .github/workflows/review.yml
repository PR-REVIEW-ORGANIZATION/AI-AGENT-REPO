name: Reusable AI PR Review

on:
  workflow_call:
    inputs:
      repository:
        description: Repository in owner/repo format
        required: true
        type: string
      pr_number:
        description: Pull request number
        required: true
        type: number
      base_branch:
        description: Base branch name
        required: true
        type: string
      head_branch:
        description: Head branch name
        required: true
        type: string
      commit_sha:
        description: Head commit SHA
        required: true
        type: string
    secrets:
      GH_TOKEN:
        required: true
      OPENAI_API_KEY:
        required: true
      LLM_API_URL:
        required: true
      LLM_MODEL:
        required: true

jobs:
  run-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Resolve agent repository source
        id: agent_source
        shell: bash
        run: |
          set -euo pipefail
          workflow_ref="${{ github.workflow_ref }}"
          repo="${workflow_ref%%/.github/workflows/*}"
          ref="${workflow_ref##*@}"
          echo "repo=$repo" >> "$GITHUB_OUTPUT"
          echo "ref=$ref" >> "$GITHUB_OUTPUT"

      - name: Checkout agent repository
        uses: actions/checkout@v4
        with:
          repository: ${{ steps.agent_source.outputs.repo }}
          ref: ${{ steps.agent_source.outputs.ref }}
          path: agent-repo

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        working-directory: agent-repo/pr-review-agent
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run PR review pipeline
        working-directory: agent-repo/pr-review-agent
        env:
          github_token: ${{ secrets.GH_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          LLM_API_URL: ${{ secrets.LLM_API_URL }}
          LLM_MODEL: ${{ secrets.LLM_MODEL }}
        run: |
          python app/main.py \
            --repository "${{ inputs.repository }}" \
            --pr-number "${{ inputs.pr_number }}" \
            --base-branch "${{ inputs.base_branch }}" \
            --head-branch "${{ inputs.head_branch }}" \
            --commit-sha "${{ inputs.commit_sha }}"

      - name: Upload PR review artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pr-review-${{ inputs.pr_number }}
          path: |
            agent-repo/pr-review-agent/outputs/Detailed_PR_Review_PR_${{ inputs.pr_number }}.docx
            agent-repo/pr-review-agent/outputs/Detailed_PR_Review_PR_${{ inputs.pr_number }}.json
          if-no-files-found: error
